<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	

<mapper namespace="com.hypeboy.codemeets.model.dao.PwFindDao">

	<select id="pwFind" parameterType="String" resultType="int">
		SELECT count(*)
		FROM user NATURAL JOIN user_info
		WHERE user_id = #{userId} and email = #{userEmail}
	</select>

	<update id="pwChange" parameterType="String">
		UPDATE user
		SET password = #{newPw}
		WHERE user_id = #{userId}
	</update>

</mapper>